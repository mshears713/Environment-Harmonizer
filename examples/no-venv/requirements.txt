# Project dependencies
# These should be installed in a virtual environment, not system-wide

# For this example, we keep it minimal to avoid external dependencies
# In a real project, you might have:
# requests>=2.28.0
# flask>=2.3.0
# pytest>=7.4.0
